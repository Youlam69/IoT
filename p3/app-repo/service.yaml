apiVersion: v1
kind: Service
metadata:
  name: aelyakou-playground
spec:
  # NodePort 30888 is published by k3d as http://localhost:8888
  type: NodePort
  selector:
    app: aelyakou-playground
  ports:
    - name: http
      port: 8888
      targetPort: http
      nodePort: 30888
